{
  "name": "firebase-ai-gemini-api",
  "version": "1.0.0",
  "description": "A ChatGPT-like application using Google's Gemini API and Firebase",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx firebase deploy"
  },
  "dependencies": {
    "@google/generative-ai": "^0.1.3",
    "firebase": "^10.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.17",
    "firebase-tools": "^13.30.0",
    "typescript": "^5.7.3",
    "vite": "^5.0.0"
  }
}
